version: '3.1'
services:
  postgres:
    image: postgres:latest 
    container_name: postgres-container 
    ports:
      - "5432:5432" 
    environment:
      POSTGRES_DB: post 
      POSTGRES_USER: jhon 
      POSTGRES_PASSWORD: 123 
    
